# API 文檔\n\n## 📋 目錄\n\n- [概述](#概述)\n- [認證](#認證)\n- [開獎數據 API](#開獎數據-api)\n- [AI 預測 API](#ai-預測-api)\n- [統計 API](#統計-api)\n- [外部網站對接 API](#外部網站對接-api)\n- [錯誤處理](#錯誤處理)\n- [速率限制](#速率限制)\n\n## 📌 概述\n\n### 基本信息\n\n- **基礎 URL**: `https://your-domain.com/api/trpc`\n- **協議**: HTTP/HTTPS\n- **格式**: JSON\n- **認證**: OAuth 2.0 + JWT\n\n### tRPC 調用方式\n\n所有 API 端點都通過 tRPC 暴露，支持以下調用方式：\n\n#### 前端 React Hook\n\n```typescript\nimport { trpc } from '@/lib/trpc';\n\n// 查詢\nconst { data, isLoading } = trpc.draw.latest.useQuery();\n\n// 變更\nconst mutation = trpc.draw.create.useMutation({\n  onSuccess: (data) => {\n    console.log('成功:', data);\n  }\n});\n\nmutation.mutate({ /* 參數 */ });\n```\n\n#### HTTP 請求\n\n```bash\n# 查詢\ncurl 'https://your-domain.com/api/trpc/draw.latest?input={}'\n\n# 變更\ncurl -X POST 'https://your-domain.com/api/trpc/draw.create' \\\n  -H 'Content-Type: application/json' \\\n  -d '{\"input\": {/* 參數 */}}'\n```\n\n## 🔐 認證\n\n### OAuth 登錄流程\n\n1. 用戶點擊登錄按鈕\n2. 重定向到 Manus OAuth 登錄頁面\n3. 用戶授權應用\n4. 重定向回應用，帶有授權碼\n5. 後端交換授權碼獲取 JWT\n6. 前端存儲 JWT 到 Cookie\n\n### 獲取當前用戶\n\n```typescript\n// 前端\nconst { data: user } = trpc.auth.me.useQuery();\n\n// 返回\n{\n  id: 'user_id',\n  name: 'User Name',\n  email: 'user@example.com',\n  role: 'user' | 'admin',\n  createdAt: '2026-03-17T12:00:00Z'\n}\n```\n\n### 登出\n\n```typescript\nconst logoutMutation = trpc.auth.logout.useMutation();\nawait logoutMutation.mutateAsync();\n```\n\n## 📊 開獎數據 API\n\n### 獲取最新開獎\n\n**端點**: `draw.latest`\n\n```typescript\nconst { data } = trpc.draw.latest.useQuery();\n\n// 返回\n{\n  drawNumber: '115015391',\n  drawTime: '2026-03-17T20:50:00Z',\n  numbers: [1, 3, 6, 11, 12, 14, 23, 24, 29, 30, 34, 35, 36, 37, 41, 46, 48, 50, 52, 54],\n  superNumber: 69,\n  bigSmall: 'big' | 'small',\n  oddEven: 'odd' | 'even',\n  plate: 'upper' | 'lower' | 'odd' | 'even',\n  timestamp: 1773752159000\n}\n```\n\n### 獲取最近 N 期開獎\n\n**端點**: `draw.recent`\n\n**參數**:\n- `limit` (number, 1-100) - 返回記錄數，預設 10\n\n```typescript\nconst { data } = trpc.draw.recent.useQuery({ limit: 20 });\n\n// 返回\n[\n  { /* 開獎記錄 1 */ },\n  { /* 開獎記錄 2 */ },\n  // ...\n]\n```\n\n### 獲取指定期數開獎\n\n**端點**: `draw.getByNumber`\n\n**參數**:\n- `drawNumber` (string) - 期數\n\n```typescript\nconst { data } = trpc.draw.getByNumber.useQuery({ \n  drawNumber: '115015391' \n});\n\n// 返回\n{ /* 開獎記錄 */ }\n```\n\n### 獲取指定日期開獎\n\n**端點**: `draw.getByDate`\n\n**參數**:\n- `dateStr` (string) - 日期 (YYYY-MM-DD)\n\n```typescript\nconst { data } = trpc.draw.getByDate.useQuery({ \n  dateStr: '2026-03-17' \n});\n\n// 返回\n[\n  { /* 開獎記錄 1 */ },\n  { /* 開獎記錄 2 */ },\n  // ...\n]\n```\n\n## 🤖 AI 預測 API\n\n### AI 一星預測\n\n**端點**: `aiStar.analyze`\n\n**參數**:\n- `sourceHour` (string) - 源時段 (HH:00)\n- `targetHour` (string) - 目標時段 (HH:00)\n- `samplePeriods` (number) - 樣本期數，預設 30\n- `manualNumbers` (number[]) - 手動輸入的球號（可選）\n\n```typescript\nconst mutation = trpc.aiStar.analyze.useMutation();\n\nawait mutation.mutateAsync({\n  sourceHour: '08:00',\n  targetHour: '09:00',\n  samplePeriods: 30,\n  manualNumbers: [1, 2, 3]\n});\n\n// 返回\n{\n  sourceHour: '08:00',\n  targetHour: '09:00',\n  candidateBalls: [15, 27, 42],\n  reasoning: '基於熱號分析...',\n  hotAnalysis: '熱號分析結果',\n  streakAnalysis: '連號分析結果',\n  accuracy: 0.75,\n  timestamp: 1773752159000\n}\n```\n\n### AI 超級獎預測\n\n**端點**: `aiSuperPrize.analyze`\n\n**參數**:\n- `sourceHour` (string) - 源時段\n- `targetHour` (string) - 目標時段\n- `manualNumbers` (number[]) - 手動輸入的球號（1-20 顆）\n\n```typescript\nconst mutation = trpc.aiSuperPrize.analyze.useMutation();\n\nawait mutation.mutateAsync({\n  sourceHour: '08:00',\n  targetHour: '09:00',\n  manualNumbers: [1, 2, 3, 4, 5]\n});\n\n// 返回\n{\n  sourceHour: '08:00',\n  targetHour: '09:00',\n  candidateBalls: [42, 69, 75],\n  reasoning: '基於超級獎分析...',\n  accuracy: 0.68,\n  timestamp: 1773752159000\n}\n```\n\n### 刪除預測\n\n**端點**: `aiStar.deletePrediction` / `aiSuperPrize.deletePrediction`\n\n**參數**:\n- `dateStr` (string) - 日期\n- `sourceHour` (string) - 源時段\n\n```typescript\nconst mutation = trpc.aiStar.deletePrediction.useMutation();\n\nawait mutation.mutateAsync({\n  dateStr: '2026-03-17',\n  sourceHour: '08:00'\n});\n```\n\n## 📈 統計 API\n\n### 獲取統計摘要\n\n**端點**: `stats.summary`\n\n**參數**:\n- `periods` (number) - 統計期數，預設 20\n\n```typescript\nconst { data } = trpc.stats.summary.useQuery({ periods: 20 });\n\n// 返回\n{\n  periods: 20,\n  bigSmall: {\n    big: 12,\n    small: 8\n  },\n  oddEven: {\n    odd: 10,\n    even: 10\n  },\n  numberFrequency: {\n    '1': 5,\n    '2': 3,\n    // ...\n  },\n  totalTrend: [\n    { drawNumber: '115015391', total: 1500 },\n    // ...\n  ]\n}\n```\n\n### 獲取準確率統計\n\n**端點**: `stats.accuracy`\n\n**參數**:\n- `days` (number) - 統計天數，預設 7\n\n```typescript\nconst { data } = trpc.stats.accuracy.useQuery({ days: 7 });\n\n// 返回\n{\n  days: 7,\n  data: [\n    {\n      date: '2026-03-17',\n      totalPredictions: 15,\n      hits: 12,\n      accuracy: '80.00'\n    },\n    // ...\n  ]\n}\n```\n\n## 🌐 外部網站對接 API\n\n### 獲取最新開獎\n\n**端點**: `external.getLatestDraws`\n\n**方法**: GET\n\n**參數**: `input={\"limit\":20}`\n\n```bash\ncurl 'https://your-domain.com/api/trpc/external.getLatestDraws?input={\"limit\":20}'\n\n# 返回\n{\n  \"result\": {\n    \"data\": {\n      \"success\": true,\n      \"data\": [\n        { /* 開獎記錄 */ },\n        // ...\n      ]\n    }\n  }\n}\n```\n\n### 獲取 AI 預測\n\n**端點**: `external.getAIPredictions`\n\n**方法**: GET\n\n**參數**: `input={\"limit\":15}`\n\n```bash\ncurl 'https://your-domain.com/api/trpc/external.getAIPredictions?input={\"limit\":15}'\n```\n\n### 獲取綜合數據\n\n**端點**: `external.getSummary`\n\n**方法**: GET\n\n**參數**: `input={}`\n\n```bash\ncurl 'https://your-domain.com/api/trpc/external.getSummary?input={}'\n\n# 返回\n{\n  \"result\": {\n    \"data\": {\n      \"success\": true,\n      \"latestDraws\": [ /* 開獎數據 */ ],\n      \"predictions\": [ /* 預測數據 */ ],\n      \"stats\": { /* 統計數據 */ }\n    }\n  }\n}\n```\n\n## ❌ 錯誤處理\n\n### 錯誤格式\n\n```typescript\n{\n  code: 'UNAUTHORIZED' | 'FORBIDDEN' | 'NOT_FOUND' | 'BAD_REQUEST' | 'INTERNAL_SERVER_ERROR',\n  message: '錯誤描述',\n  data?: {\n    code: 'CUSTOM_ERROR_CODE',\n    details: { /* 詳細信息 */ }\n  }\n}\n```\n\n### 常見錯誤\n\n| 代碼 | HTTP 狀態 | 說明 |\n|------|----------|------|\n| `UNAUTHORIZED` | 401 | 未認證 |\n| `FORBIDDEN` | 403 | 無權限 |\n| `NOT_FOUND` | 404 | 資源不存在 |\n| `BAD_REQUEST` | 400 | 請求參數錯誤 |\n| `INTERNAL_SERVER_ERROR` | 500 | 服務器錯誤 |\n\n### 前端錯誤處理\n\n```typescript\nconst mutation = trpc.draw.create.useMutation({\n  onError: (error) => {\n    console.error('錯誤代碼:', error.code);\n    console.error('錯誤信息:', error.message);\n    \n    if (error.code === 'UNAUTHORIZED') {\n      // 重定向到登錄\n    } else if (error.code === 'BAD_REQUEST') {\n      // 顯示驗證錯誤\n    }\n  }\n});\n```\n\n## 🚦 速率限制\n\n### 限制規則\n\n- **認證用戶**: 1000 請求/小時\n- **未認證用戶**: 100 請求/小時\n- **外部 API**: 500 請求/小時\n\n### 響應頭\n\n```\nX-RateLimit-Limit: 1000\nX-RateLimit-Remaining: 999\nX-RateLimit-Reset: 1773756000\n```\n\n### 超限響應\n\n```json\n{\n  \"code\": \"TOO_MANY_REQUESTS\",\n  \"message\": \"請求過於頻繁，請稍後再試\",\n  \"data\": {\n    \"retryAfter\": 60\n  }\n}\n```\n\n## 📚 SDK 和客戶端\n\n### JavaScript/TypeScript\n\n```typescript\nimport { trpc } from '@/lib/trpc';\n\n// 已內置 tRPC 客戶端\n```\n\n### Python\n\n```python\nimport requests\n\nresponse = requests.get(\n    'https://your-domain.com/api/trpc/draw.latest',\n    params={'input': '{}'}\n)\ndata = response.json()\n```\n\n### cURL\n\n```bash\ncurl -X GET 'https://your-domain.com/api/trpc/draw.latest?input={}' \\\n  -H 'Authorization: Bearer YOUR_JWT_TOKEN'\n```\n\n## 🔗 相關資源\n\n- [tRPC 文檔](https://trpc.io/)\n- [OAuth 2.0 規範](https://tools.ietf.org/html/rfc6749)\n- [REST API 最佳實踐](https://restfulapi.net/)\n"
